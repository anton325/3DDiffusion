wenn das importing nicht läuft:
rm /home/giese/.cache/torch_extensions/py310_cu118/_C/lock

man muss darauf achten, --tasks-per-node= auf die anzahl GPUs zu setzen, wenn man alles von einer node aus trainieren möchte

--mem= um GB memory einzustellen

--cpus= Anzahl CPUs die man will

--gpus= Anzahl GPUs die man will